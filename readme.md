arrow的目录结构

```
arrow
│  api.py # 实现了get、now、utcnow函数
│  arrow.py # Arrow类
│  constants.py # 一些常量
│  factory.py # 工厂模式
│  formatter.py # 输出格式化
│  locales.py # 时区
│  parser.py # 解析
│  util.py # 其他时间相关的函数
│  _version.py # 版本号
└─ __init__.py # 提供了一个统一的入口，简化了包的导入和使用
```

